<h1>投稿一覧</h1>

<%= link_to "投稿画面へ", new_list_path %>


<% @lists.each do |list| %>

  <p>タイトル</p>
  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>

  <P>画像</P>
    <%= image_tag list.image, size: "200x200" %>

<% end %>

